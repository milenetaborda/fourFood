import { ChatActionTypes } from '../../../models/reducer';

export default function signup(
  state: any[] = [],
  action: ChatActionTypes,
): any {
  switch (action.type) {
    default:
      return state;
  }
}
